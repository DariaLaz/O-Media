using System.Text;
using System.Text.RegularExpressions;

namespace OMedia.Extensions
{
    public class ModelExtensions
    {
        //public string GetInformation(string address, string name)
        //{
        //    StringBuilder sb = new StringBuilder();
        //    sb.Append(name.Replace(" ", "-"));
        //    sb.Append("-");
        //    sb.Append(GetAddress(address));

        //    return sb.ToString();
        //}

        //private static string GetAddress(string address)
        //{
        //    string result = string
        //        .Join("-", address.Split(" ", StringSplitOptions.RemoveEmptyEntries).Take(3));

        //    return Regex.Replace(address, @"[^a-zA-Z0-9\-]", string.Empty);
        //}
    }
}
